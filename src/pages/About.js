import React from 'react';

function About() {
  return (
    <main style={{ maxWidth: 800, margin: '0 auto', padding: '24px 16px', color: '#e6e9ff' }}>
      <h2>À propos</h2>
      <p>
        MovieScope est une application de démonstration pour explorer des concepts
        React : listes & keys, rendu conditionnel, routing, et différents styles.
      </p>
      <p>
        Design moderne, responsive, combinant CSS classique, CSS Modules, et CSS-in-JS.
      </p>
    </main>
  );
}

export default About;

